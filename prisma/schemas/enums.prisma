// ============================================
// ENUMS - Tipos enumerados compartilhados
// ============================================

enum TipoLancamento {
  pagamento // Pagamento único realizado
  agendamento // Agendamento futuro (pode ter parcelas)
}
